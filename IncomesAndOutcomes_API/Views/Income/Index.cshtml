@model IEnumerable<IncomesAndOutcomes_API.Models.Income>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th></th>
        <th>
            AccountBalance
        </th>
        <th>
            MonthBuffer
        </th>
        <th>
            Amount
        </th>
        <th>
            Date
        </th>
        <th>
            Memo
        </th>
        <th>
            IsDeleted
        </th>
        <th>
            AvailableNextMonth
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IncomeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.IncomeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IncomeId })
        </td>
        <td>
			@Html.DisplayTextFor(_ => item.AccountBalance).ToString()
        </td>
        <td>
			@Html.DisplayTextFor(_ => item.MonthBuffer).ToString()
        </td>
        <td>
			@item.Amount
        </td>
        <td>
			@String.Format("{0:g}", item.Date)
        </td>
        <td>
			@item.Memo
        </td>
        <td>
			@item.IsDeleted
        </td>
        <td>
			@item.AvailableNextMonth
        </td>
    </tr>
}

</table>


